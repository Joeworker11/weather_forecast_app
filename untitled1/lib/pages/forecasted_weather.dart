import 'package:flutter/material.dart';

class ForecastedWeather extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Előjelzett időjárás jelentés'),
      ),
      body: Container(),
    );
  }
}
