import 'package:flutter/material.dart';

class RollingShutter extends StatelessWidget {
  const RollingShutter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Árnyékolástechnika'),
      ),
      body: Container(),
    );
  }
}