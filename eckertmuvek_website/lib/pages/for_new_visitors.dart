import 'package:flutter/material.dart';

class ForNewVisitors extends StatelessWidget {
  const ForNewVisitors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Új látogatóknak'),
      ),
      body: Container(),
    );
  }
}