import 'package:flutter/material.dart';
import 'Screen/Homepage.dart';

Class Myapp extends StatelessWidget{
  MyApp{Key? key}
}: super(key: key);
@override
Widget build(BuildContext context){
  return MaterialApp(debugShowChecked)
}